// const logURL = (requestDetails: any) => { console.log(requestDetails.requestHeaders) }
// browser.webRequest.onBeforeSendHeaders.addListener(logURL, { urls: ['https://*.mozilla.org/*'] }, ['requestHeaders'])

export {};
