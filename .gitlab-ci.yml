pages:
    stage: deploy
    script:
        - rm -rf public && mkdir public
        - cp -r docs/* public/ && cp -r README.md public/_media/
    artifacts:
        paths: [public]
        expire_in: 1 mins
